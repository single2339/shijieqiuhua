"""OSINT analysis stages (W1 skeleton).

W2 will host the profiling, scoring, confidence, prediction, and report
modules currently embedded in pipeline.py. W1 keeps this package as a
placeholder so PRs touching analysis logic land here from the start.

PRD trace: §5.1 spec backend layout (analysis/{profiling,factor_scoring,
confidence,prediction,report}.py).
"""
